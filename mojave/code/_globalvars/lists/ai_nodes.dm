GLOBAL_LIST_EMPTY(allnodes)

// AI EDIT: moved here from mojave/code/modules/mob/robots/trader.dm, which is excluded from the build (it
// duplicates DD's own /mob/living/simple_animal/friendly/trader) - effect_node.dm still needs this list though.
GLOBAL_LIST_EMPTY(nodes_trader_destination)

///A GLOB of all /datum/component/ai_controller that currently exist
GLOBAL_LIST_EMPTY(ai_instances_active)

//To be implemented in later updates
//GLOBAL_LIST_EMPTY(nodes_with_enemies)
//GLOBAL_LIST_EMPTY(nodes_with_construction)
